@page
@model MusicEventManagementSystem.Pages.Admin.Events.IndexModel
@{
    ViewData["Title"] = "Event Management";
}

<h1>Event Management</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New Event</a>
</p>

<table class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Name</th>
            <th>Location</th>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Published</th>
            <th>Actions</th> [cite: 209]
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.MusicEvents)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.EventName)</td>
                <td>@Html.DisplayFor(modelItem => item.Location)</td>
                <td>@Html.DisplayFor(modelItem => item.EventDate)</td> [cite: 210]
                <td>@Html.DisplayFor(modelItem => item.EndDate)</td>
                <td>@Html.DisplayFor(modelItem => item.IsPublished)</td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.EventID" class="btn btn-sm btn-outline-secondary">Edit</a>
                    <a asp-page="./Delete" asp-route-id="@item.EventID" class="btn btn-sm btn-outline-danger">Delete</a> [cite: 211]
                    <a asp-page="/Events/Detail" asp-route-id="@item.EventID" class="btn btn-sm btn-outline-info" target="_blank">View</a> [cite: 211]
                </td>
            </tr>
        }
    </tbody>
</table>